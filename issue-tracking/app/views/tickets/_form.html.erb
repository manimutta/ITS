
        <%= form_for(@ticket) do |f| %>
          <% if @ticket.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>
              <ul>
              <% @ticket.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>

          <div class="field">
            <%= f.text_field :title, class: "input-no-material",placeholder: "Title" %>
          </div>
          <br>
          <div class="field">
            <%= f.text_area :description, placeholder: "ENter the description",  class: "input-no-material"%>
          </div>
          <br>
          <div class="actions">
            <button class="btn btn-primary btn-raised" type="submit">Submit</button>

        <%= link_to 'Back', tickets_path, class: "btn btn-primary" %>
          </div>
        <% end %>
      